[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_functions = test_*
filterwarnings =
    ignore::DeprecationWarning:pydantic.*
markers =
    llm: tests appelant les APIs LLM (nécessitent OPENAI_API_KEY en CI)
